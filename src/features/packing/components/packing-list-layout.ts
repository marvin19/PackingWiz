/** Horizontal padding for packing item rows inside a category container. */
export const PACKING_ITEM_CARD_PADDING_HORIZONTAL = 12;

/** Matches PackingItemRow settings button (iconButton width/height). */
export const PACKING_ITEM_ACTION_SIZE = 32;

/** Rounded container wrapping all items in one category section. */
export const PACKING_CATEGORY_CONTAINER_RADIUS = 16;
